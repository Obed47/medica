version: '3'

services:

    web:
    
      build: .
      #command: python3 manage.py runserver 0.0.0.0:8000

      volumes:
        - .:/app

      ports:
        - "8001:8001"

      entrypoint: /app/entrypoint.sh gunicorn 